# 游戏对局行为预测技术报告

## 1. 总体技术路线与方法选择

本项目解决的是一个典型的时序行为决策分类问题：在给定主玩家过去 20 秒状态轨迹的前提下，预测其下一步动作类别。该任务有两个明显难点：

1. 输入来源复杂，包含多种异构日志（动作、伤害、技能、空间位置信息等）。
2. 标签分布不均衡，少数类（如 Grenade、BeingResuce）容易被主流类别淹没。

针对上述问题，采用稳健、可解释、切实落地的路线：

1. 先把原始日志转成结构化表格特征。
2. 用树模型（LightGBM / XGBoost）学习非线性关系。
3. 用 macro_f1 作为核心评价指标，确保模型不只服务头部类别。
4. 用训练元信息约束推理特征对齐，防止线上线下口径偏移。

一句话概括：这是一个以工程可复现为核心、以少数类识别为导向、以表格树模型为主干的完整方案。


---

## 2. 数据处理与特征构建（build_features_v2.py）

### 2.1 数据概况

#### 数据来源

- 训练数据来源：

  `classified_samples_0、classified_samples_2、classified_samples_3`
- 验证数据来源：`classified_samples_1`
- 测试数据来源：`测试1000题`

注：综合考虑训练时间、分类效果等因素，其中classified_samples_0有大约280000份样本，最终只取了96532份样本。

#### 数据规模

|数据集 |样本数 |特征数 |
|---|---|---|
|训练集 |129308 |139 |
|验证集 |10749 |139 |
|测试集 |1000 |138 |

#### 标签分布

在原始标签分布中，不同类别样本数量存在明显差异，其中头部类别占比更高，尾部类别样本相对较少。为避免模型只偏向头部类别，本方案在训练阶段采用 `macro_f1` 作为主要评价指标，并结合类别不平衡处理策略。

|类别 |样本数 |占比 |
|---|---|---|
|Fire |37826 |29.25% |
|Action |37070 |28.67% |
|SkillStart |33762 |26.11% |
|Looting |12022 |9.30% |
|Grenade |6994 |5.41% |
|BeingResuce |1634 |1.26% |


---

### 2.2 为什么先做特征表

原始 txt 日志天然是事件流格式，直接建模成本高且不稳定。将其转换为统一的样本级特征表，有三点收益：

1. 训练与推理流程可以完全复用。
2. 特征可审计，便于后续错误分析与迭代。
3. 更适合 LightGBM/XGBoost 这类对结构化数据友好的模型。

### 2.3 解析策略与标签策略

脚本按日志类型逐类解析：

- 玩家基础信息
- 动作
- 技能生效
- 玩家造成伤害
- 玩家击倒
- 玩家死亡
- 决策行

标签策略上，虽然支持从决策行解析，但在批处理时默认优先使用目录名作为标签（label_from_folder=True）。这样做的实用意义是：即便个别日志中的决策字段异常，也不会把标签污染扩散到整个训练集。

### 2.4 主玩家识别与防泄漏

主玩家识别分两层：

1. 优先取决策行 main_player_id。
2. 缺失时从 action.player_id、damage.attacker_id、skill.caster_player_id 里按频次兜底。

泄漏控制分两层：

1. 时间边界：严格只取决策时刻及之前数据。
2. 下游再剔除高风险字段（见训练阶段），双保险避免模型偷看答案。

### 2.5 特征体系

当前特征设计围绕“状态、强度、趋势、关系”四个维度展开：

1. 末态特征：决策前瞬时状态（位置、视角、速度、开镜、可见性）。
2. 多时间窗统计：20/10/5/3 秒的均值、方差、计数等，表达节奏强弱。
3. 分段趋势：20 秒前后半段差值，识别行为切换前兆。
4. 动作模式：动作频次、切换次数、当前动作与上一动作。
5. 战斗态势：输入/输出伤害量、伤害可见比例、攻防净值。
6. 技能互动：施放与受击技能计数、buff 多样性、最近技能间隔。
7. 生存事件：击倒/死亡相关计数与最近发生间隔。
8. 空间博弈：最近敌人距离、平均距离、路径效率与位移特征。

这些特征共同回答一个问题：玩家现在是“在交战推进、在规避拉扯，还是在切换到资源/技能行为”。

### 2.6 批量构建策略

为了兼顾规模与类别平衡，脚本支持每类采样上限 per_class_max（默认 30000），并输出各类采样数量与最终形状。这种控制对训练稳定性很关键，尤其是在长尾类别本来就少的情况下。


---

## 3. 模型训练与选择（optimized_training_notebook.ipynb）

### 3.1 训练流程设计思路

训练阶段不是“直接调参”，而是分层推进：

1. 先做可解释的 baseline，建立性能基线。
2. 再做特征增强与参数搜索，验证提升是否真实。
3. 用统一交叉验证框架比较模型，避免单次切分偶然性。
4. 最后保存可复现产物，确保推理阶段零歧义。

### 3.2 数据清洗与高风险字段剔除

训练时显式删除：

- sample_id
- main_player_id
- decision_time
- decision_skill_name
- decision_effect_radius

其中后两者属于强泄漏风险字段：它们可能直接编码了目标动作信息。剔除后虽然短期指标可能降低，但泛化更可信。

### 3.3 缺失值处理

- 数值列：常数填补 -999（显式编码“事件未发生/缺失”）。
- 类别列：众数填补 + OneHotEncoder(handle_unknown=ignore, min_frequency=10)。
- 验证方式：5 折 StratifiedKFold。
- 主指标：macro_f1。
  - 选择 macro_f1 的原因很直接：它对每个类别等权，能真实反映长尾类是否被学到。
- 缺失率过高列：当缺失率超过 `[填写阈值，默认99%]` 时直接删除。
  - 具有缺失值的特征和其缺失值占比如下（缺失值占比为100%属数据处理疏漏，重新处理数据耗时太大便在训练代码中去除。）：

|特征名称 |缺失值占比 |
|---|---|
|last_knock_in_gap |1.000000 |
|last_knock_out_gap |1.000000 |
|last_death_in_gap |0.999899 |
|last_visible_state |0.978965 |
|decision_effect_radius |0.949949 |
|last_death_out_gap |0.935696 |
|last_damage_in_gap |0.877146 |
|damage_in_visible_ratio |0.877146 |
|last_damage_out_gap |0.730125 |
|damage_out_visible_ratio |0.730125 |
|last_skill_hit_gap |0.472098 |
|last_skill_cast_gap |0.442154 |
|current_action_time |0.098153 |
|current_action_gap_to_decision |0.098153 |
|nearest_player_dist |0.060553 |
|mean_player_dist |0.060553 |
|seg_fov_mean_diff |0.056114 |
|seg_scope_ratio_diff |0.056114 |
|seg_speed_mean_diff |0.056114 |
|seg1_scope_ratio |0.056083 |

### 3.4 二次特征工程与类别压缩

在基础特征上继续构造比值、净值、密度、趋势绝对差等特征，并对低频类别值做压缩（映射为 **OTHER**）。

目的有两个：

1. 让模型看到“变化”而不仅是“水平值”。
2. 控制 one-hot 稀疏维度膨胀，降低过拟合风险。

### 3.5 模型策略：Baseline + Optuna + 融合

训练支持三类候选：

1. LightGBM（稳定、快、常见强基线）。
2. XGBoost（鲁棒性强，参数空间灵活）。
3. Soft Voting（可选概率融合）。

调参使用 Optuna，目标函数为 5 折 macro_f1。流程上还支持开关化实验：可以独立开关 LGB/XGB 搜索，控制试验成本。

### 3.6 不平衡强化（这一步很关键）

在 class_weight=balanced 之外，额外叠加两层样本权重：

1. 小类增强权重（如 BeingResuce、Grenade）。
2. 业务分组权重（交战/避战）。

这使得训练目标更贴近比赛关注点，而不是单纯追求总体准确率。

### 3.7 最终模型导出与可复现性

最终导出：

- final_pipeline.joblib
- label_encoder.joblib
- training_meta.json

其中 training_meta.json 记录特征列、删列策略、最优参数、最终模型名称，是推理阶段一致性的核心依赖。


---

## 4. 推理预测与提交构建（inference_notebook_final.ipynb）

### 4.1 推理阶段的核心原则

推理效果的上限由训练决定，但推理稳定性由“口径一致性”决定。本项目推理流程重点解决的是后者。

### 4.2 推理流程

1. 加载 final_pipeline、label_encoder、training_meta。
2. 读取测试特征文件。
3. 执行与训练一致的显式特征工程。
4. 按 training_meta 的特征清单严格对齐列名和顺序。
5. 输出类别、置信度、Top3 与各类概率。

列对齐策略：

- 缺失列补 NaN。
- 多余列删除。
- 顺序按训练列重排。

这一步直接避免了“训练时能跑、上线时报错”或“无声性能退化”的典型问题。

### 4.3 输出与提交

默认导出：

- predictions_full.csv / xlsx（全量分析版）
- submission_basic.csv / xlsx（基础提交版）
- submission_competition.csv / xlsx（比赛格式版）

同时完成业务映射：

- Looting、BeingResuce -> 避战
- 其他类别 -> 交战

并支持动作行为列的二次格式化（中文到英文标签映射）。

### 4.4 可选本地评估

若测试文件包含 label，可直接输出：

- accuracy
- macro_f1
- classification_report
- 交战/避战二分类指标

这为提交前离线自检提供了非常实用的快速回路。


---

## 5. 实验结果与解读

### 最终模型选择

根据交叉验证结果，最终选择 XGBoost 作为提交模型。

模型model_₀23在样本sample_₁上的表现：

|指标 |数值 |
|:---|:---|
|Validation Accuracy |0.8353 |
|Validation Macro F1 |0.7473 |


---

## 🎯 分类性能详情

|类别 |Precision |Recall |F1-Score |Support |
|:---|:---:|:---:|:---:|:---:|
|Action |0.7212 |0.9463 |0.8186 |2851 |
|BeingResuce |0.8744 |0.9784 |0.9235 |185 |
|Fire |0.8442 |0.6900 |0.7593 |2771 |
|Grenade |0.3559 |0.4565 |0.4000 |46 |
|Looting |0.9944 |0.9401 |0.9665 |3756 |
|SkillStart |0.6868 |0.5579 |0.6157 |1140 |
|**Macro Avg** |0.7461 |0.7615 |0.7473 |10749 |
|**Weighted Avg** |0.8458 |0.8353 |0.8335 |10749 |


---

## 🎯 交战/避战 判别

|指标 |数值 |
|:---|:---|
|Intent Accuracy |0.9770 |
|Intent Macro F1 |0.9750 |

|Intent |Precision |Recall |F1-Score |Support |
|:---|:---:|:---:|:---:|:---:|
|交战 |0.9692 |0.9953 |0.9821 |6808 |
|避战 |0.9915 |0.9454 |0.9679 |3941 |

### 5.1 基线表现

Baseline macro_f1 scores（F1分数）:
[0.73964631 0.73699898 0.7370733 0.73749535 0.73747801]

Baseline macro_f1 mean:
0.7377383901396004

说明：在未进行 XGBoost 深度调参前，模型已具备可用区分能力，但对长尾类仍不够敏感。

### 5.2 XGBoost 最优参数（50次搜索）

Best XGB score:
0.8784320039807281

Best XGB params:

- n_estimators: 843
- learning_rate: 0.10898273956734476
- max_depth: 10
- min_child_weight: 6
- subsample: 0.9623633236981084
- colsample_bytree: 0.8057540010479758
- gamma: 0.03337124927144871
- reg_alpha: 0.34628509535468843
- reg_lambda: 7.894494892102157

### 5.3 5 折交叉验证对比

LightGBM:

- F1-Macro mean: 0.7377
- Accuracy mean: 0.8161

XGBoost（最佳）:

- F1-Macro mean: 0.8784
- Accuracy mean: 0.8911

SoftVoting(

SOFT_VOTING_WEIGHTS = (0.45, 0.55)  # (LightGBM, XGBoost)

):

- F1-Macro mean: 0.8780
- Accuracy mean: 0.8877

结论：本轮实验中，XGBoost 在 macro_f1 上最优，且优于融合模型，说明其单模型决策边界已经较充分。

### 5.4 在 sample_1 上的推理评估

- Validation accuracy（六分类准确率）: 0.8353335193971533
- Validation macro_f1: 0.7472538564472987
- Intent accuracy（交战/避战 二分类准确率）: 0.9770211182435575
- Intent macro_f1: 0.9750090318010041

解读：细粒度 6 类行为仍存在混淆空间，但在交战/避战这一级业务决策上，模型已达到较高可用性。


---

## 6.其他处理

### 6.1类别不平衡处理

为缓解少数类学习不足问题，训练阶段采用以下方法：

- 使用 `class_weight='balanced'` 或样本权重。
- 在模型评估中以 `macro_f1` 为主。
- 在错误分析中重点关注尾部类别。

#### 增强训练

对数据较少的三类进行训练增强

RARE_CLASS_WEIGHT_MULTIPLIER = {
'BeingResuce': 1.8,
'Grenade': 2.4,
'SkillStart': 1.2
}

### 6.2 业务导向惩罚：提升“交战/避战”组别学习强度

INTENT_GROUP_MULTIPLIER = {
'交战': 1.2,
'避战': 1.6
}


---

## 7. 风险与下一步优化方向

当前主要风险：

1. 尾部类别样本仍少，跨局势泛化可能不稳。
2. 特征虽丰富，但仍以人工构造为主，存在表达上限。
3. 细粒度类别间语义接近时，单阶段分类仍会混淆。

后续优化顺序：

1. 先做严格消融实验，确定真正有效的特征与权重策略。
2. 再做两阶段分类（先交战/避战，再细分动作）。
3. 最后尝试融合扩展（不同种子、不同特征子集）提升稳定性。


---

## 8. 结论

本方案已经形成从数据解析、特征构建、模型训练到推理提交的完整工程闭环。

从现有结果看：

1. 数据处理链路稳定，具备批量构建能力。
2. XGBoost 在当前任务上取得最佳综合表现。
3. 推理端通过特征对齐和元信息复用，具备较好的上线可靠性。


---

## 9.其他尝试

### ==基于 transformer 的游戏行为时序预测（由于对深度学习和transformer架构不太熟悉、训练条件有限、时间紧张等因素，只做了一个很简单的预测模型，不作为最终方案。）==

### 绝地求生时序行为预测

### 9.1 方案概述

本方案采用**“数据构建—时序特征提取—序列分类训练—测试推理导出”**的完整技术流程，实现从原始日志到行为分类的端到端处理。核心模块由以下三个 Python 脚本组成：

- `build_torch.py`：从原始 txt 日志中解析上下文信息，构建 PyTorch 可直接加载的 `dataset.pt` 数据集，并同步导出样本元信息文件 `meta.csv`。
- `torch_train.py`：加载 `dataset.pt`，基于 LSTM 或 Transformer 编码器完成监督训练，保存最优模型 `best_model.pt`、归一化统计量 `norm_stats.pt`、训练日志 `train_log.csv` 及标签映射信息。
- `infer_torch.py`：读取训练产物，对测试集样本逐批次推理，输出分类结果并导出提交文件。

### 数据处理

### 主玩家识别

主玩家识别采用**启发式多源打分策略**，综合以下信息源按优先级确定主玩家：

|优先级 |信息源 |权重说明 |
|:---:|:---|:---|
|1 |决策行中直接标注的主玩家标识 |最高优先级，直接采用 |
|2 |动作日志中出现频率最高的玩家 |反映主动行为参与度 |
|3 |伤害日志中攻击者/受害者高频玩家 |反映战斗参与情况 |
|4 |技能施法者中出现频率最高的玩家 |反映技能使用主动性 |
|5 |玩家基础信息中综合出现频率最高的玩家 |作为兜底判定依据 |

对上述各信息源的频次进行加权求和，得分最高的玩家即认定为主玩家。

### 主时序特征

主时序特征直接来源于主玩家的基础状态字段，包括：

|特征名 |含义 |来源 |
|:---|:---|:---|
|`x, y, z` |三维位置坐标 |原始日志字段 |
|`weapon_yaw, weapon_pitch` |武器朝向（偏航角/俯仰角） |原始日志字段 |
|`vx, vy, vz` |三维速度分量 |原始日志字段 |
|`fov` |视野角度 |原始日志字段 |
|`speed` |速度标量 |由 $\sqrt{v_x^2 + v_y^2 + v_z^2}$ 计算得到 |
|`is_scoped` |是否处于开镜状态 |由开镜状态字段按规则匹配判定 |
|`is_visible` |是否可见 |由可见性相关字段抽取判定 |

**统一序列长度处理**：为适配批量训练，所有时间序列统一至固定长度 `seq_len`：

- **长度不足时**：序列前端补零（pre-padding），使长度等于 `seq_len`
- **长度超长时**：对序列进行等距抽样（uniform sampling），确保信息均匀覆盖

### 辅助统计特征

除主时序序列外，方案额外构建一组**辅助统计特征**，用于补充主玩家在近期时间窗口内的行为模式语义：

|时间窗口 |统计维度 |具体指标 |
|:---:|:---|:---|
|20s / 10s / 5s |记录密度 |各窗口内日志记录条数 |
|20s / 10s / 5s |速度统计 |平均速度、速度标准差 |
|20s / 10s / 5s |开镜行为 |开镜记录比例 |
|全局 |动作统计 |动作次数、技能释放次数 |
|全局 |伤害统计 |造成伤害次数、受到伤害次数、伤害总量 |
|全局 |击杀/死亡 |击杀次数、死亡次数、击倒次数、被击倒次数 |

该设计遵循**“时序特征 + 统计特征”融合范式**，时序特征保留动态变化信息，统计特征补充高层战术语义，两者互补提升分类性能。

### 当前特征清单

|特征类型 |主要内容 |说明 |
|:---|:---|:---|
|主时序特征 |`x, y, z, weapon_yaw, weapon_pitch, vx, vy, vz, fov, speed, is_scoped, is_visible` |共 13 维，由主玩家基础状态直接构成 |
|派生特征 |`speed, is_scoped, is_visible` |由原始日志字段计算或规则抽取得到 |
|辅助统计特征 |20s/10s/5s 窗口统计、伤害统计、动作统计等 |共 26+ 维，补充高层行为语义 |


---

## 模型设计

### 总体结构

训练脚本支持两类时序编码模型：**LSTM 分类器**和**Transformer 分类器**。两种模型均遵循统一的 **序列编码器 + 辅助特征 MLP + 分类头** 融合架构：

1. 对主玩家时序特征进行编码，得到时序全局表示
2. 将时序表示与辅助统计特征的 MLP 输出进行拼接
3. 拼接向量经分类头输出各类别 logits

### LSTM 版本

> ⚠️ **弃用**：LSTM 版本实验效果不佳，此处仅记录架构。

**数据流**：

```text
输入序列 → 线性映射 → LayerNorm → GELU激活 → 多层双向LSTM → 带mask平均池化 → 时序全局表示
```

编码后的隐藏状态通过**带 mask 的平均池化**聚合为固定维度的全局表示，随后与辅助特征 MLP 输出拼接，进入分类头预测。

### Transformer 版本

**数据流**：

```text
输入序列 → 线性投影 + 位置编码 → 多层Transformer Encoder → 带mask平均池化 → 时序全局表示
```

Transformer 编码器通过**自注意力机制**建模长距离时间依赖，能够有效捕获时序中的关键行为模式。由于输入序列经过前置零填充处理，训练与推理阶段均启用 **padding mask**，避免填充时刻的无效信息干扰有效表示学习。


---

## 实验配置

### 数据设置

|数据集用途 |来源目录 |
|:---|:---|
|训练集 |`classified_samples_1`、`classified_samples_2` |
|验证集 |`classified_samples_3` |
|测试集 |`测试1000题` |

### 模型参数

|参数类别 |参数名 |数值 |
|:---:|:---|---:|
|**基础配置** |model_name |`transformer` |
|   |epochs |`80` |
|   |batch_size |`32` |
|   |lr |`3e-4` |
|   |weight_decay |`1e-3` |
|   |patience |`20` |
|   |val_ratio |`0.15` |
|   |seed |`42` |
|**模型结构** |hidden_dim / d_model |`192` |
|   |num_layers |`3` |
|   |dropout |`0.2` |
|   |aux_hidden |`64` |
|   |label_smoothing |`0.03` |
|**Transformer 专用** |nhead |`8` |
|   |ffn_dim |`512` |

#### 性能优化配置

|优化项 |参数 |数值 |
|:---|:---|---:|
|损失函数 |use_focal_loss |`True` |
|   |focal_gamma |`1.5` |
|模型集成 |use_ema |`True` |
|   |ema_decay |`0.999` |
|混合精度 |use_amp |`True` |
|梯度累积 |accum_steps |`4` |
|学习率预热 |warmup_epochs |`10` |

#### 数据增强配置

|增强方式 |参数 |数值 |
|:---|:---|---:|
|高斯噪声 |use_augmentation |`True` |
|   |aug_noise_std |`0.02` |
|随机掩码 |aug_mask_ratio |`0.03` |
|随机缩放 |aug_scale_range |`0.1` |

#### 对比学习辅助损失

|参数 |数值 |
|:---|---:|
|use_supcon |`True` |
|supcon_weight |`0.05` |
|supcon_temp |`0.07` |


---

## 实验结果

### Action-level 分类指标

|类别 |Precision |Recall |F1-score |Support |
|:---|---:|---:|---:|---:|
|Fire |0.8496 |0.5531 |0.6700 |1215 |
|Grenade |0.0755 |0.0556 |0.0640 |72 |
|SkillStart |0.4123 |0.6426 |0.5024 |582 |
|Looting |0.9530 |0.8270 |0.8855 |1029 |
|BeingResuce |0.9290 |0.9386 |0.9338 |1255 |
|Action |0.5584 |0.7772 |0.6499 |615 |

**Action-level 汇总指标**：

|指标 |数值 |
|:---|---:|
|Accuracy |**0.7460** |
|Macro Precision |0.5397 |
|Macro Recall |0.5420 |
|Macro F1 |**0.5294** |
|Weighted Precision |0.7902 |
|Weighted Recall |0.7460 |
|Weighted F1 |**0.7537** |

### 分类指标（交战/避战二分类）

|类别 |Precision |Recall |F1-score |Support |
|:---|---:|---:|---:|---:|
|交战 |0.9333 |0.9795 |0.9558 |2484 |
|避战 |0.9764 |0.9238 |0.9494 |2284 |

**Intent-level 汇总指标**：

|指标 |数值 |
|:---|---:|
|Accuracy |**0.9528** |
|Macro Precision |0.9548 |
|Macro Recall |0.9516 |
|Macro F1 |**0.9526** |
|Weighted Precision |0.9539 |
|Weighted Recall |0.9528 |
|Weighted F1 |**0.9527** |

### 总体结果汇总

|指标层级 |Accuracy |Macro-F1 |Weighted-F1 |
|:---|---:|---:|---:|
|Action-level |0.7460 |0.5294 |0.7537 |
|Intent-level |0.9528 |0.9526 |0.9527 |


